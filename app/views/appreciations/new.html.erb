<div class="max-w-7xl mx-auto">
  <div class="mb-6 md:mb-8">
    <h1 class="text-2xl md:text-3xl font-bold text-gray-900 mb-2">Kudosを送る</h1>
    <p class="text-sm md:text-base text-gray-600">チームメンバーに感謝の気持ちを伝えましょう</p>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
    <!-- メインフォーム（左側、2/3幅） -->
    <div class="lg:col-span-2">
      <%= turbo_frame_tag "appreciation_form" do %>
        <%= render "form", appreciation: @appreciation, users: @users, offices: @offices %>
      <% end %>
    </div>

    <!-- 最近送信したユーザー（右側、1/3幅） -->
    <div class="lg:col-span-1">
      <div class="bg-white border border-gray-200 shadow-sm rounded-xl p-6 sticky top-6">
        <h3 class="text-lg font-bold text-gray-900 mb-4">最近送信したユーザー</h3>

        <% if @recent_recipients.any? %>
          <div class="space-y-3">
            <% @recent_recipients.each do |user| %>
              <button
                type="button"
                class="recent-user-btn w-full flex items-center gap-3 p-3 rounded-lg border border-gray-200 hover:bg-gray-50 hover:border-gray-300 transition-all text-left"
                data-user-id="<%= user.id %>"
                data-name="<%= user.full_name %>"
                data-avatar="<%= user.last_name[0] %>"
                data-location="<%= "#{user.office&.name} - #{user.department&.name} - #{user.section&.name}" %>">
                <div class="flex items-center justify-center w-10 h-10 rounded-full bg-blue-100 text-blue-700 font-semibold text-sm">
                  <%= user.last_name[0] %>
                </div>
                <div class="flex-1 min-w-0">
                  <div class="font-medium text-gray-900 truncate"><%= user.full_name %></div>
                  <div class="text-xs text-gray-500 truncate">
                    <%= user.office&.name || '所属なし' %>
                  </div>
                </div>
                <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-gray-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="m9 18 6-6-6-6"/>
                </svg>
              </button>
            <% end %>
          </div>
        <% else %>
          <div class="text-center py-8">
            <div class="w-12 h-12 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-3">
              <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 text-gray-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/>
                <circle cx="9" cy="7" r="4"/>
                <path d="M22 21v-2a4 4 0 0 0-3-3.87"/>
                <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
              </svg>
            </div>
            <p class="text-sm text-gray-500">まだ誰にも送信していません</p>
          </div>
        <% end %>

        <div class="mt-6 pt-6 border-t border-gray-200">
          <p class="text-xs text-gray-500">クリックすると、そのユーザーを選択できます</p>
        </div>
      </div>
    </div>
  </div>
</div>